# D&D Tracker Server Environment Configuration

# Application Environment
NODE_ENV=development

# Server Configuration
PORT=5000
CORS_ORIGIN=http://localhost:3000

# Database Configuration (MongoDB Atlas)
# Production: Use MongoDB Atlas connection string
# Development: Use local MongoDB instance
DATABASE_URL=mongodb://localhost:27017/dnd-tracker

# Redis Configuration (Required for Production)
# Production: Use Redis Cloud or production Redis instance
# Development: Optional, will use in-memory rate limiting if not provided
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=redispassword

# Session Configuration
SESSION_SECRET=your-session-secret-here

# Production Environment Requirements:
# - NODE_ENV must be set to "production"
# - DATABASE_URL is required (MongoDB Atlas connection string)
# - REDIS_URL is required (Redis Cloud or production Redis instance)
# - REDIS_PASSWORD is optional (defaults to "redispassword")
#
# The application will perform startup checks in production and exit with
# a non-zero code if Redis connection cannot be established.